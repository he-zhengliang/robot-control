#!/usr/bin/env python3
# -*- coding: utf-8 -*-

import rospy
import numpy as np
import serial
import time
import threading
from geometry_msgs.msg import PointStamped, PoseStamped
import moveit_commander
from tf.transformations import quaternion_from_euler, quaternion_matrix
import matplotlib.pyplot as plt
from matplotlib.animation import FuncAnimation

# =========================================================
# CONFIGURATION
# =========================================================
GRIPPER_PORT = "/dev/ttyACM0"
GRIPPER_BAUD = 9600
TACTILE_PORT = "/dev/ttyACM1"
TACTILE_BAUD = 250000

FORCE_THRESHOLD = 0.5  
TAIL_LENGTH = 25       
CHANNELS = 12

# Colors (RGB)
COLOR_GREEN = (0, 0.7, 0)
COLOR_RED   = (0.9, 0, 0)
COLOR_IDLE  = (0.7, 0.7, 0.7) # Gray

# =========================================================
# TACTILE DATA ENGINE
# =========================================================

class TactileSystem:
    def __init__(self, port, baud):
        self.lock = threading.Lock()
        self.current_forces = np.zeros(CHANNELS)
        
        # Sensor Geometry: Ro=5.0, Ri=2.0
        self.Ro, self.Ri = 5.0, 2.0
        self.rc = (2/3) * (self.Ro**3 - self.Ri**3) / (self.Ro**2 - self.Ri**2)
        theta_deg = [0, 90, 180, 270]
        self.xs = self.rc * np.cos(np.deg2rad(theta_deg))
        self.ys = self.rc * np.sin(np.deg2rad(theta_deg))

        # Trajectory History
        self.history_x = [[] for _ in range(3)]
        self.history_y = [[] for _ in range(3)]

        try:
            self.ser = serial.Serial(port, baud, timeout=0.1)
            rospy.loginfo("[Tactile] Connected.")
        except:
            self.ser = None

        self.thread = threading.Thread(target=self._update_loop)
        self.thread.daemon = True
        self.thread.start()

    def _update_loop(self):
        while not rospy.is_shutdown():
            if self.ser and self.ser.in_waiting > 0:
                line = self.ser.readline().decode(errors="ignore").strip()
                values = line.split(",")
                if len(values) >= 13:
                    try:
                        # Extract 12 normalized capacitance values (c1-c12)
                        floats = [float(x) for x in values[1:13]]
                        with self.lock:
                            self.current_forces = np.array(floats)
                    except:
                        continue

    def get_data(self):
        with self.lock:
            return self.current_forces.copy(), self.history_x, self.history_y

# =========================================================
# ANIMATION & COLOR LOGIC
# =========================================================

tactile = TactileSystem(TACTILE_PORT, TACTILE_BAUD)

fig, axs = plt.subplots(1, 3, figsize=(15, 5), facecolor='white')
sensor_names = ['Index Finger', 'Middle Finger', 'Ring Finger']
h_cops, h_tails = [], []
circle_t = np.linspace(0, 2*np.pi, 200)

for i, ax in enumerate(axs):
    ax.set_xlim(-6, 6); ax.set_ylim(-6, 6); ax.set_aspect('equal')
    # Draw Sensor Board
    ax.plot(tactile.Ro*np.cos(circle_t), tactile.Ro*np.sin(circle_t), 'k', lw=1.5)
    ax.plot(tactile.Ri*np.cos(circle_t), tactile.Ri*np.sin(circle_t), 'k', lw=1.5)
    ax.set_title(sensor_names[i])
    
    line, = ax.plot([], [], lw=2, alpha=0.6)
    dot = ax.scatter([0], [0], s=130, edgecolors='black', zorder=5) 
    h_tails.append(line)
    h_cops.append(dot)

def update(frame):
    forces, hx, hy = tactile.get_data()
    
    for s in range(3):
        f_segment = forces[s*4 : (s+1)*4]
        f_sum = np.sum(f_segment)
        
        if f_sum > FORCE_THRESHOLD:
            # --- ACTIVE CONTACT ---
            xc = np.sum(f_segment * tactile.xs) / f_sum
            yc = np.sum(f_segment * tactile.ys) / f_sum
            
            hx[s].append(xc)
            hy[s].append(yc)
            if len(hx[s]) > TAIL_LENGTH:
                hx[s].pop(0); hy[s].pop(0)
            
            h_tails[s].set_data(hx[s], hy[s])
            h_cops[s].set_offsets([[xc, yc]])
            
            # Distance from center
            r = np.sqrt(xc**2 + yc**2)
            
            # Logic: Center is GREEN, Edge is RED
            current_color = COLOR_GREEN if r <= 1.5 else COLOR_RED
            h_cops[s].set_facecolor(current_color)
            h_tails[s].set_color(current_color)
            
        else:
            # --- IDLE STATE ---
            h_cops[s].set_offsets([[0, 0]])
            h_cops[s].set_facecolor(COLOR_IDLE)
            hx[s].clear()
            hy[s].clear()
            h_tails[s].set_data([], [])

    return h_cops + h_tails

ani = FuncAnimation(fig, update, interval=50, blit=True)
plt.tight_layout()
plt.show()
